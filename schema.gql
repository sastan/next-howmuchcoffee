type Coffee {
  amount: Int!
  date: Time!
  notes: String
  person: Person
}

type Person {
  name: String!
  age: Int!
  email: String!
  coffees: [Coffee] @relation
}

type Query {
  person: [Person]
}
